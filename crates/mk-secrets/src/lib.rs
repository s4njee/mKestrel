//! `mk-secrets` — platform secure storage (iOS Keychain / Secure Enclave,
//! Android Keystore) and biometric gating (Epic 12).
//!
//! Scaffold crate until E12. The database never stores secret material — only
//! opaque IDs into whatever this crate backs.

pub fn placeholder() {}
